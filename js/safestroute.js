//
// safestroute.js
// This is the heavyweight file, this takes in a route (or creates a route) and outputs a matrix of GPS coordinates along the route.
// Assume: 
//	- A route currently exists on the map
//	- It is the only route on the map
//	- The route contains at least several data points of which we have data
//

$(document).ready(function() {
	// Basic Functionality:
	// A route is created or exists on the map
	// Create a list of 
})